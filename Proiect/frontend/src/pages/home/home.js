let Home = {
    render: async() => {
        return `
           
            
           
            <a href="/login">login</a>
        `
    },
    after_render: async () => {}
}

export default Home